ProcessConfig = uBBBundler
{
  AppTick   = 10
  CommsTick = 10

  debug = false

  // Geodesy conversion disabled - pBB_DGPS_EKF handles NAV_X/NAV_Y filtering
  // lat_var          = NAV_LAT
  // lon_var          = NAV_LONG
  // nav_x_out_var    = NAV_X
  // nav_y_out_var    = NAV_Y

  // Only forward attitude variables - pBB_DGPS_EKF handles NAV_HEADING, NAV_SPEED, NAV_COG
  forward_map = NAV_ROLL_NAVIGATOR:NAV_ROLL,NAV_PITCH_NAVIGATOR:NAV_PITCH,NAV_YAW_NAVIGATOR:NAV_YAW
}
